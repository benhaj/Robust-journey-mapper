name: "base"
channels:
  - defaults
dependencies:
  - numpy
  - pandas
  - networkx
  - matplotlib
  - ipywidgets
  - plotly=4.14.3
  - hdfs3
  - pip
  - pip:
      - scikit-learn
      - geopandas
      - geopy
# - add packages here
# - one per line
prefix: "/opt/conda"

